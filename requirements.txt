fastapi==0.110.3
uvicorn[standard]==0.30.1
sqlmodel==0.0.16
# SQLModel 0.0.16 expects SQLAlchemy 1.x (DOUBLE type); pin to avoid import errors
SQLAlchemy==1.4.52
pydantic==1.10.15
httpx==0.27.0
obspy==1.4.0
torch==2.2.2
# pinned versions kept in sync with refreshed backend modules
